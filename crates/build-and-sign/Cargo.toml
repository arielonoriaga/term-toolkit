[package]
name = "ttk-build-and-sign"
version = "0.1.0"
edition = "2021"

[dependencies]
walkdir = "2"
zip = "2"
ttk-core = { path = "../core" }
ttk-sign = { path = "../sign" }

[dev-dependencies]
tempfile = "3"
